/** Line colours for multi-series charts, in order, so every trend chart looks the same. */
export const LINE_COLORS = [
  "var(--primary)",
  "var(--btn-success)",
  "var(--destructive)",
  "var(--btn-highlight)",
  "var(--primary-medium)",
  "var(--primary-dark)",
];
